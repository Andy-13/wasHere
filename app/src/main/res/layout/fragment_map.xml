<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
    <com.amap.api.maps.MapView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/mapView"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_alignParentBottom="true"
        android:background="@drawable/map_gradient"/>


    <ImageButton
        android:layout_width="@dimen/map_btn_small_size"
        android:layout_height="@dimen/map_btn_small_size"
        android:id="@+id/btnRefresh"
        android:scaleType="centerInside"
        android:src="@drawable/map_refresh_btn"
        android:background="#00000000"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="50dp"
        android:layout_marginRight="@dimen/map_btn_margin_size"
        android:layout_marginEnd="@dimen/map_btn_margin_size"
        android:layout_toLeftOf="@+id/btnSendMessage"
        android:layout_toStartOf="@+id/btnSendMessage"/>

    <ImageButton
        android:layout_width="@dimen/map_btn_big_size"
        android:layout_height="@dimen/map_btn_big_size"
        android:id="@+id/btnSendMessage"
        android:scaleType="centerInside"
        android:src="@drawable/map_send_btn"
        android:background="#00000000"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="35dp"
        android:layout_centerHorizontal="true"/>

    <ImageButton
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:id="@+id/btnSetRange"
        android:scaleType="centerInside"
        android:src="@drawable/map_search_btn"
        android:background="#00000000"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="50dp"
        android:layout_marginLeft="@dimen/map_btn_margin_size"
        android:layout_marginStart="@dimen/map_btn_margin_size"
        android:layout_toRightOf="@+id/btnSendMessage"
        android:layout_toEndOf="@+id/btnSendMessage"/>





</RelativeLayout>